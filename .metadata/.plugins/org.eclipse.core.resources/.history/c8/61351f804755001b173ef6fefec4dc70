<!doctype html>
<html>
<head>
<meta charset="UTF-8">
<title>prompt입력받기</title>
<link rel="stylesheet" href="../css/public.css">
  <script>
    function proc1(){
    	
        //입력하기 - prompt()
        a = prompt("이름을 입력하세요", "홍길동");
        
        //document.write(a);
        
        //div에 출력하기 위해서 htmldiv요소에 접근
        tag = document.getElementsByTagName('div')[0];
        
        //tag에 출력 - innerHTML, textContent
        //innerHTML : 출력하고 하는 태그쪽에 HTML이 포함되어 있으면 사용
        //textContent : 출력하고 하는 태그쪽에 HTML이 포함되어 있지 않으면 사용
        //tag.textContent = a;
        //tag.textContent = "<h1>" + a + "</h1>";
        tag.innerHTML = "<h1>" + a + "</h1>";
    }
  </script>
</head>
<body>
<pre>
  script에서 html요소에 접근
  
  1) document.getElementsByTagName('div')[0];
  
  // ECMAScript(es6)
  2) document.querySelector('div') : 여러개 있어도 첫번째 요소로 접근
  3) document.querySelectorAll('div') : 모든 div로 접근
  => querySelector를 사용할때는 아이디를 찾을때는 #, 클래스를 사용할때 .을 사용해야 한다.
  => document.querySelector('#aa') / document.querySelector('.aa')
  => getElementById를 통해 가져올 수 있음
  => 실행속도면에서는 1번형식이 2-3번형식보다 빠르다.
  => 자바에서도 향상된 for문보다 인덱스를 통해 for문을 사용하는 것이 속도면에서 빠르다.
</pre>
	<input type="button" value="확인" onclick="proc1()">
	<div></div>
</body>
</html>