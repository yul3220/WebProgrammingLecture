<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
<link rel="stylesheet" href="../css/public.css">
<script>
function proc1(){
	
	// 두개의 수와 연산자 한개를 입력
	x = parseInt(prompt("정수1 입력"));
	y = parseInt(prompt("정수2 입력"));
	
	op = prompt("연산자 입력", "+,-,*,/");
	
	//비교(if문)
	/*if(op == "+"){
		res = x + y;
	}else if(op == "-"){
		res = x - y;
	}else if(op == "*"){
		res = x * y;
	}else if(op == "/"){
		res = x / y;
	}*/
	
	//비교(switch문)
	switch(op){
	case "+" :
		res = x + y;
		break;
	case "-" :
		res = x - y;
		break;
	case "*" :
		res = x * y;
		break;
	case "/" :
		res = x / y;
		break;
		
	}
	//백틱
	str=`${x} ${op} ${y} = ${res}`;
	
	//출력
	dd = document.getElementsByTagName('div')[0];
	dd.innerHTML = str;
	
	text = document.getElementById('result');
	text.value = res;
}
</script>
</head>
<body>
<pre>
백틱  BackQuote, backtic, graveaccent
하나의 문자열 안에서 변수와 문자열을 동시에 나타낸다.
변수를 나타낼때는 ${변수}

백틱을 이용하여 문자열을 표현하는 것을 템플릿 리터럴 이라고 한다.
ES 6표준이다.
</pre>
<input type="button" value="확인" onclick="proc1()">
<br><br>
<div></div>
결과 <input type="text" id="result">
</body>
</html>